<h1>New post</h1>

<%= form_with(model: [@user, @post], url: user_posts_path(user_id: @user)) do |f| %>
  <div>
    <%= f.label :title %> <br>
    <%= f.text_field :title %>
  </div>
  <div>
    <%= f.label :text %> <br>
    <%= f.text_area :text %>
  </div>
  <%= f.submit %>
<% end %>
